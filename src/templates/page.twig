{% extends '_layouts/cp' %}
{% import '_includes/forms.twig' as forms %}

{% set title = 'Mailpit'|t('mailpit') %}
{% set pluginCpSection = settings.getType() == 'page' %}

{% set crumbs = [
	{ label: 'Mailpit'|t('mailpit'), url: url('mailpit') },
] %}

{% block content %}
	{% include 'mailpit/components/frame' with { source: settings.getHost() } only %}
{% endblock %}
